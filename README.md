# Turjeeman Django Server

## Usage

```
# To run on localhost
# Install the packages (dependent libraries) before running:
pip -r requirements.txt
npm install
# Run the application:
./node_modules/.bin/webpack --config webpack.config.js --watch
django-admin.py runserver
```
